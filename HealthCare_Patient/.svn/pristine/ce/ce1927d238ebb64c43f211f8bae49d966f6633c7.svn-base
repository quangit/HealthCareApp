namespace HealthCare.DependencyServices
{
    public interface IImageResize
    {
        byte[] ResizeImage(byte[] imageData, float width, float height);
        byte[] ResizeImageAvatar(byte[] imageData, float width, float height);
    }
}